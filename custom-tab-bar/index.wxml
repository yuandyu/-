<van-tabbar active="{{ selected }}">
  <van-tabbar-item wx:for="{{list}}" wx:key="index" icon="{{item.iconPath}}" data-path="{{item.pagePath}}" data-index="{{index}}" bindtap="switchTab" >{{item.text}}</van-tabbar-item>
</van-tabbar>
